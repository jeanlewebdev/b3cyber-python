install pip package manager

use pip to install the virtual environment module

use the venv module to create a virtual env

source the virtual env

use pip to install the requirements from the requirements.txt file

test-run the app using python appli.py
